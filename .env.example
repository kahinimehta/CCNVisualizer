# Copy this file to `.env` in the repo root (`.env` is gitignored — never commit it).
#
#   cp .env.example .env
#   # edit .env and paste your key
#
# Get an API key: https://console.anthropic.com/settings/keys

ANTHROPIC_API_KEY=sk-ant-api03-REPLACE_ME
ANTHROPIC_MODEL=claude-opus-4-6

# Optional: exit on first classification failure (default: skip failed rows)
# LLM_THEME_STRICT=1
